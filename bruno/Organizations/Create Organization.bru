meta {
  name: Create Organization
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v1/organizations
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Rivus Pay"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
